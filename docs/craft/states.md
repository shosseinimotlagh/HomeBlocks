# CRAFT LSN State Machines

> **This page has moved.** The slot and write state machine (Appended, Committed,
> Missing, Empty), the write kinds (data vs `all_zeros` zero write), the per-write readability
> model, holes in reads, and the read-eligibility rule (LBA-overlap, the login-dLSN `L` split,
> journal-tail overlay reads) are now maintained in the canonical
> [**CRAFT Design**](https://github.com/eBay/HomeBlocks/wiki/CRAFT-Design) wiki page (with the
> HomeStore binding on [**CRAFT on HomeBlocks**](https://github.com/eBay/HomeBlocks/wiki/CRAFT-on-HomeBlocks)).
> See its Glossary and the "Write, Commit, Read" and Reconfiguration sections.

This stub remains only so existing links do not break.
